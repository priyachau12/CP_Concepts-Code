#include <bits/stdc++.h>
using namespace std;
#define int long long

int solve()
{
    int n, res=0, len=1, trans=0;
    cin>>n;
    string s;
    cin>>s;
    vector<int>dp(n);
    
}

int32_t main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int tc;
    cin >> tc;
    while (tc--)
    {
        solve();
    }
}

