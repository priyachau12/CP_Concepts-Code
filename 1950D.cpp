#include<bits/stdc++.h>
using namespace std;

vector<bool> bin(5*10e5,false);

bool checkbind(int n)
{
    while(n>0)
    {
        if(n%10)
    }
}

int precomp()
{
    for(int i=0;i<10e5 + 1;i++)
    {
        if(checkbind(i))
        {
            bin[i] = true;
        }
    }
}